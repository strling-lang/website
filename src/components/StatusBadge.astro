---
interface Props {
  label: string;
  tone?: 'published' | 'source' | 'prepared' | 'prerelease';
}

const { label, tone = 'source' } = Astro.props;
---

<span class:list={['status-badge', `status-badge--${tone}`]}>{label}</span>
